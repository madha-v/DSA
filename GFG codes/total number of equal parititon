

class Solution {
  public:
  long long prefixSum(int n) {
    long long res = 0;
    for (int k = 1; k <= n; k++) {
        res += 1LL * k * (n / k);
    }
    return res;
}

    long long partitions(int l, int r) {
         return prefixSum(r) - prefixSum(l - 1);
    }
};
